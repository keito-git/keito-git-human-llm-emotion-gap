# Requirements for human_vs_ai_emotion experiments
# Python >= 3.10

# Data handling
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0

# Configuration
pyyaml>=6.0

# Statistics & metrics
scipy>=1.11.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0

# API clients (Phase 2)
openai>=1.30.0
anthropic>=0.25.0

# Inter-annotator agreement
krippendorff>=0.6.0

# --- Optional dependencies ---
# Required only for analysis/finetuned_baseline.py (RoBERTa baseline)
# torch>=2.0.0
# transformers>=4.40.0
#
# Required only for llm_inference/run_vllm_inference.py (open-weight inference)
# vllm>=0.5.0
#
# Required only if you use lexical_transparency.py with sentence-transformers embeddings
# sentence-transformers>=2.7.0
